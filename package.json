{
  "scripts": {
    "dev": "bunx --bun concurrently -n \"frontend,backend\" -c \"green,red\" \"cd frontend && bun run dev\" \"cd backend && .\\venv\\Scripts\\activate && python scripts.py start\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7"
  }
}
